spring:
  application:
    name: api-gateway
  
ribbon:
   eureka:
      enabled: true
      
eureka:
  instance:
      hostname: localhost
      preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
